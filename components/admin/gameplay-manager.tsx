"use client";
import {useState} from "react";
type Chest={key:string;name:string;enabled:boolean;cost_axp:number;reward_pool:unknown};
type Season={key:string;name:string;starts_at:string;ends_at:string;enabled:boolean;reward_config:unknown;event_config:unknown};
export function GameplayManager({initialChests,initialSeasons}:{initialChests:Chest[];initialSeasons:Season[]}){const[chests,setChests]=useState(initialChests),[seasons,setSeasons]=useState(initialSeasons),[message,setMessage]=useState("");async function patch(body:Record<string,unknown>){const response=await fetch("/api/admin/aion",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),data=await response.json();if(!response.ok)throw new Error(data.error||"Update failed.");return data;}async function editChest(chest:Chest){try{const name=prompt("Chest name",chest.name),costAxp=Number(prompt("AXP cost",String(chest.cost_axp))),pool=prompt("Reward pool JSON",JSON.stringify(chest.reward_pool));if(!name||!pool)return;const data=await patch({type:"chest",key:chest.key,name,costAxp,enabled:chest.enabled,rewardPool:JSON.parse(pool)});setChests(current=>current.map(value=>value.key===chest.key?data.chest:value));setMessage("Chest configuration saved and audited.");}catch(error){setMessage(error instanceof Error?error.message:"Update failed.");}}async function editSeason(season:Season){try{const name=prompt("Season name",season.name),startsAt=prompt("Starts at (ISO)",new Date(season.starts_at).toISOString()),endsAt=prompt("Ends at (ISO)",new Date(season.ends_at).toISOString());if(!name||!startsAt||!endsAt)return;const data=await patch({type:"season",key:season.key,name,startsAt,endsAt,enabled:season.enabled,rewardConfig:season.reward_config,eventConfig:season.event_config});setSeasons(current=>current.map(value=>value.key===season.key?data.season:value));setMessage("Season configuration saved and audited.");}catch(error){setMessage(error instanceof Error?error.message:"Update failed.");}}return <section className="bg-[#020711] px-5 pb-10 text-white sm:px-8"><div className="mx-auto max-w-7xl rounded-2xl border border-white/10 bg-[#081321] p-5"><p className="text-[10px] uppercase tracking-[.2em] text-cyan-300">Live-ops configuration</p><h2 className="mt-1 text-xl">Mystery chests, seasons & events</h2>{message&&<p className="mt-3 text-xs text-cyan-200">{message}</p>}<div className="mt-4 grid gap-4 lg:grid-cols-2"><div><h3 className="text-sm">Chests</h3>{chests.map(chest=><button key={chest.key} onClick={()=>editChest(chest)} className="mt-2 block w-full rounded-xl border border-white/10 p-3 text-left text-xs"><b>{chest.name}</b> · {chest.cost_axp} AXP · {chest.enabled?"active":"disabled"}</button>)}</div><div><h3 className="text-sm">Seasons and limited events</h3>{seasons.map(season=><button key={season.key} onClick={()=>editSeason(season)} className="mt-2 block w-full rounded-xl border border-white/10 p-3 text-left text-xs"><b>{season.name}</b> · {season.enabled?"active":"disabled"}<span className="mt-1 block text-slate-500">{new Date(season.starts_at).toLocaleString()} – {new Date(season.ends_at).toLocaleString()}</span></button>)}{!seasons.length&&<p className="mt-2 text-xs text-slate-500">Create the first season through the audited admin API, then manage it here.</p>}</div></div></div></section>}
