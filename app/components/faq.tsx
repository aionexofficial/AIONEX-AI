"use client";
import { useState } from "react";
const questions = [["What is AIONEX AI?", "AIONEX AI is a community-led ecosystem bringing intelligent tools and transparent on-chain infrastructure together."], ["What is the AIONEX token used for?", "The token is designed for ecosystem access, governance participation, and network incentives."], ["Where can I follow the project?", "Join the AIONEX community channels for product updates, ecosystem news, and upcoming announcements."], ["How can I get involved?", "Follow the roadmap, join the community, and watch for ecosystem and governance opportunities."]];
export function Faq() { const [active, setActive] = useState<number | null>(0); return <section id="faq" className="mx-auto max-w-3xl scroll-mt-24 px-6 py-24"><div className="text-center"><p className="text-sm font-semibold tracking-[.2em] text-cyan-300">FAQ</p><h2 className="mt-3 text-4xl font-bold tracking-tight sm:text-5xl">Questions, answered.</h2></div><div className="mt-12 space-y-3">{questions.map(([question, answer], index) => <div key={question} className="rounded-2xl border border-white/10 bg-white/[.04] px-5 backdrop-blur-sm"><button className="flex w-full items-center justify-between gap-6 py-5 text-left font-medium" onClick={() => setActive(active === index ? null : index)} aria-expanded={active === index}><span>{question}</span><span className="text-2xl font-light text-cyan-300">{active === index ? "−" : "+"}</span></button>{active === index && <p className="pb-5 pr-8 text-sm leading-7 text-slate-400">{answer}</p>}</div>)}</div></section>; }
