import { AppShell } from "../components/app-shell";import { FeatureStatus } from "@/components/system/feature-status";
export default function Staking(){return <AppShell eyebrow="AIONEX STAKING" title="Staking"><FeatureStatus title="Staking is not live" description="No audited staking contract or production reward schedule is configured. The application does not advertise simulated yields or accept staking transactions."/></AppShell>}
