import { randomBytes } from "node:crypto";import { cookies } from "next/headers";
export async function GET(){const id=process.env.GOOGLE_CLIENT_ID,secret=process.env.GOOGLE_CLIENT_SECRET;if(!id||!secret)return Response.json({error:"Google authentication is not configured."},{status:503});const state=randomBytes(24).toString("base64url"),redirect=process.env.GOOGLE_USER_REDIRECT_URI||`${process.env.NEXT_PUBLIC_SITE_URL}/api/user-auth/google/callback`;(await cookies()).set("aionex_google_state",state,{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:600});const query=new URLSearchParams({client_id:id,redirect_uri:redirect,response_type:"code",scope:"openid email profile",state,prompt:"select_account"});return Response.redirect(`https://accounts.google.com/o/oauth2/v2/auth?${query}`)}
