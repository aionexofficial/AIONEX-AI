import "server-only";

import { neon } from "@neondatabase/serverless";
import { deleteNarration, storeNarration } from "@/lib/automation/audio-assets";
import type { StoryScenario } from "./types";

const database=()=>{if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL is not configured.");return neon(process.env.DATABASE_URL);};
async function narration(text:string,voice:string){if(!process.env.OPENAI_API_KEY)throw new Error("OPENAI_API_KEY is required for Story Engine narration.");const response=await fetch("https://api.openai.com/v1/audio/speech",{method:"POST",signal:AbortSignal.timeout(90000),headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_TTS_MODEL||"gpt-4o-mini-tts",voice,instructions:"Speak as AION: professional, calm, confident, trustworthy, and clear. Pronounce AIONEX as eye-on-ex, AION as eye-on, AXP as A-X-P, and Web3 as web three.",input:text,response_format:"mp3"})});if(!response.ok)throw new Error(`OpenAI narration failed (${response.status}): ${(await response.text()).slice(0,250)}`);return Buffer.from(await response.arrayBuffer());}

function renderElements(scenario:StoryScenario,audioUrl:string){const elements:Record<string,unknown>[]=[{type:"audio",source:audioUrl},{type:"shape",shape:"rectangle",x:"50%",y:"50%",width:"100%",height:"100%",fill_color:"#030814"},{type:"shape",shape:"ellipse",x:"50%",y:"38%",width:"62%",height:"35%",fill_color:"#0B2640",stroke_color:"#22D3EE",stroke_width:"0.4 vmin",shadow_color:"#22D3EE",shadow_blur:"5 vmin",animations:[{type:"scale",scope:"element",start_scale:"80%",end_scale:"110%",duration:scenario.durationSeconds}]},{type:"shape",shape:"ellipse",x:"50%",y:"38%",width:"22%",height:"12%",fill_color:"#07101F",stroke_color:"#8B5CF6",stroke_width:"0.5 vmin",animations:[{type:"spin",duration:scenario.durationSeconds}]},{type:"text",text:"AIONEX",x:"50%",y:"8%",width:"88%",height:"7%",fill_color:"#67E8F9",font_family:"Arial",font_weight:"700",font_size:"5 vmin",x_alignment:"50%",y_alignment:"50%",animations:[{type:"fade",duration:.4}]},{type:"text",text:scenario.thumbnailText,x:"50%",y:"18%",width:"86%",height:"13%",fill_color:"#FFFFFF",font_family:"Arial",font_weight:"700",font_size:"6 vmin",x_alignment:"50%",y_alignment:"50%",animations:[{type:"slide",direction:"up",duration:.45}]}];for(const scene of scenario.scenes){elements.push({type:"text",text:scene.onScreenText,time:scene.startSeconds,duration:scene.endSeconds-scene.startSeconds,x:"50%",y:"50%",width:"84%",height:"17%",fill_color:"#A5F3FC",stroke_color:"#020617",stroke_width:"0.35 vmin",font_family:"Arial",font_weight:"700",font_size:"6 vmin",x_alignment:"50%",y_alignment:"50%",animations:[{type:"fade",duration:.25},{type:"slide",direction:"up",duration:.35}]});elements.push({type:"text",text:scene.subtitleText,time:scene.startSeconds,duration:scene.endSeconds-scene.startSeconds,x:"50%",y:"78%",width:"86%",height:"18%",fill_color:"#FFFFFF",stroke_color:"#020617",stroke_width:"0.55 vmin",font_family:"Arial",font_weight:"700",font_size:"5.1 vmin",x_alignment:"50%",y_alignment:"50%",animations:[{type:"fade",duration:.18}]});}return elements;}

async function createRender(scenario:StoryScenario,audioUrl:string){if(!process.env.CREATOMATE_API_KEY)throw new Error("CREATOMATE_API_KEY is required for Story Engine rendering.");const response=await fetch("https://api.creatomate.com/v2/renders",{method:"POST",signal:AbortSignal.timeout(30000),headers:{Authorization:`Bearer ${process.env.CREATOMATE_API_KEY}`,"Content-Type":"application/json"},body:JSON.stringify({output_format:"mp4",width:1080,height:1920,duration:scenario.durationSeconds,frame_rate:30,background_color:"#030814",elements:renderElements(scenario,audioUrl)})});const body=await response.json() as Array<{id?:string;status?:string;url?:string;error_message?:string}>|{id?:string;status?:string;url?:string;error_message?:string};if(!response.ok)throw new Error(`Creatomate rejected Story Engine render (${response.status}).`);let job=Array.isArray(body)?body[0]:body;if(!job?.id)throw new Error("Creatomate returned no Story Engine render ID.");for(let attempt=0;attempt<48&&job.status!=="succeeded";attempt++){if(job.status==="failed")throw new Error(job.error_message||"Creatomate Story Engine render failed.");await new Promise(resolve=>setTimeout(resolve,5000));const poll=await fetch(`https://api.creatomate.com/v2/renders/${job.id}`,{headers:{Authorization:`Bearer ${process.env.CREATOMATE_API_KEY}`},signal:AbortSignal.timeout(20000)});job=await poll.json() as typeof job;}if(job.status!=="succeeded"||!job.url)throw new Error("Creatomate Story Engine render timed out.");return{id:String(job.id),url:job.url};}

export async function validateStoryOutput(input:{url:string;scenario:StoryScenario;width:number;height:number;hasAudio:boolean;hasSubtitles:boolean}){const errors:string[]=[];if(input.width!==1080||input.height!==1920)errors.push("Output must be 1080x1920 (9:16).");if(input.scenario.durationSeconds<30||input.scenario.durationSeconds>60)errors.push("Duration must be between 30 and 60 seconds.");if(!input.hasAudio)errors.push("Audio is missing.");if(!input.hasSubtitles||input.scenario.scenes.some(scene=>!scene.subtitleText.trim()))errors.push("Subtitles are missing.");if(input.scenario.scenes.some(scene=>!scene.backgroundTemplateKey||!scene.motionInstructions.length||!scene.visualInstructions.length))errors.push("A scene is visually incomplete.");if(!input.scenario.scenes.some(scene=>/aion|aionex/i.test(`${scene.onScreenText} ${scene.visualInstructions.join(" ")}`)))errors.push("AIONEX identity is missing.");try{const response=await fetch(input.url,{method:"HEAD",signal:AbortSignal.timeout(20000)});const type=response.headers.get("content-type")||"";const length=Number(response.headers.get("content-length")||0);if(!response.ok||!type.includes("video"))errors.push("Rendered video URL is unavailable or unsupported.");if(length>0&&length<100000)errors.push("Rendered video appears corrupted or empty.");}catch{errors.push("Rendered video URL could not be validated.");}return{passed:errors.length===0,errors,width:input.width,height:input.height,aspectRatio:"9:16",duration:input.scenario.durationSeconds,hasAudio:input.hasAudio,hasSubtitles:input.hasSubtitles,hasLogo:true,noBlankScenes:input.scenario.scenes.every(scene=>scene.visualInstructions.length>0&&scene.motionInstructions.length>0)};}

export async function renderPrivateStoryPreview(scenarioId:string,voice="alloy"){const sql=database(),scenarioRows=await sql`SELECT * FROM story_scenarios WHERE id=${scenarioId}::uuid AND status='approved' LIMIT 1`;if(!scenarioRows[0])throw new Error("Approved Story Engine scenario not found.");const sceneRows=await sql`SELECT * FROM story_scenes WHERE scenario_id=${scenarioId}::uuid ORDER BY position`;const row=scenarioRows[0],scenario:StoryScenario={scenarioKey:String(row.scenario_key),categoryKey:String(row.category_key),topic:String(row.topic),title:String(row.title),hook:String(row.hook),narration:String(row.narration),presenter:String(row.presenter) as StoryScenario["presenter"],durationSeconds:Number(row.duration_seconds),thumbnailText:String(row.thumbnail_text),telegramCaption:String(row.telegram_caption),youtubeTitle:String(row.youtube_title),youtubeDescription:String(row.youtube_description),xCaption:String(row.x_caption),hashtags:Array.isArray(row.hashtags)?row.hashtags.map(String):[],callToAction:String(row.call_to_action),factualSafetyNotes:Array.isArray(row.factual_safety_notes)?row.factual_safety_notes.map(String):[],sourceReferences:Array.isArray(row.source_references)?row.source_references.map(String):[],scenes:sceneRows.map(scene=>({position:Number(scene.position),purpose:String(scene.purpose),startSeconds:Number(scene.start_seconds),endSeconds:Number(scene.end_seconds),narration:String(scene.narration),onScreenText:String(scene.on_screen_text),subtitleText:String(scene.subtitle_text),backgroundTemplateKey:String(scene.background_template_key),visualInstructions:(scene.visual_instructions as string[])||[],motionInstructions:(scene.motion_instructions as string[])||[],assetReferences:(scene.asset_references as string[])||[]}))};const key=`story-preview:${scenarioId}:${createKey(scenario)}`,existing=await sql`SELECT * FROM story_render_jobs WHERE idempotency_key=${key} LIMIT 1`;if(existing[0]?.status==="ready")return{job:existing[0],skipped:true,reason:"Already rendered"};const jobs=await sql`INSERT INTO story_render_jobs(scenario_id,idempotency_key,presenter,status,attempts) VALUES(${scenarioId}::uuid,${key},${scenario.presenter},'narrating',1) ON CONFLICT(idempotency_key) DO UPDATE SET attempts=story_render_jobs.attempts+1,status='narrating',last_error=NULL,updated_at=NOW() RETURNING *`;const jobId=String(jobs[0].id);let assetId:string|undefined;try{const audio=await narration(scenario.narration,voice),asset=await storeNarration(audio);assetId=asset.id;await sql`UPDATE story_render_jobs SET status='rendering',audio_asset_id=${asset.id}::uuid,updated_at=NOW() WHERE id=${jobId}::uuid`;const render=await createRender(scenario,asset.url);await sql`UPDATE story_render_jobs SET provider_job_id=${render.id},video_url=${render.url},status='validating',updated_at=NOW() WHERE id=${jobId}::uuid`;const validation=await validateStoryOutput({url:render.url,scenario,width:1080,height:1920,hasAudio:true,hasSubtitles:true});if(!validation.passed)throw new Error(`Story output validation failed: ${validation.errors.join(" ")}`);const done=await sql`UPDATE story_render_jobs SET status='ready',duration_seconds=${scenario.durationSeconds},width=1080,height=1920,has_audio=TRUE,has_subtitles=TRUE,validation=${JSON.stringify(validation)}::jsonb,completed_at=NOW(),updated_at=NOW() WHERE id=${jobId}::uuid RETURNING *`;await sql`UPDATE story_scenarios SET status='previewed',updated_at=NOW() WHERE id=${scenarioId}::uuid`;await sql`INSERT INTO story_deliveries(scenario_id,platform,status,external_id,external_url,attempts,updated_at) VALUES(${scenarioId}::uuid,'preview','skipped',${render.id},${render.url},1,NOW()) ON CONFLICT(scenario_id,platform) DO UPDATE SET status='skipped',external_id=EXCLUDED.external_id,external_url=EXCLUDED.external_url,attempts=story_deliveries.attempts+1,last_error=NULL,updated_at=NOW()`;return{job:done[0],validation,skipped:false};}catch(error){const message=error instanceof Error?error.message:"Story render failed";await sql`UPDATE story_render_jobs SET status='failed',last_error=${message.slice(0,2000)},updated_at=NOW() WHERE id=${jobId}::uuid`;throw error;}finally{if(assetId)await deleteNarration(assetId).catch(()=>undefined);}}
function createKey(scenario:StoryScenario){return Buffer.from(`${scenario.scenarioKey}:${scenario.title}:${scenario.narration.length}`).toString("base64url").slice(0,80);}
