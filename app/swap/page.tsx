import { AppShell } from "../components/app-shell";import { FeatureStatus } from "@/components/system/feature-status";
export default function Swap(){return <AppShell eyebrow="AIONEX SWAP" title="Swap"><FeatureStatus title="Swap execution is not configured" description="No router contract, liquidity source, allowance flow, or quote provider is configured. This surface remains disabled to prevent misleading or unsafe transactions."/></AppShell>}
