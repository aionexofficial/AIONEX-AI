import { AppShell } from "../components/app-shell";import { FeatureStatus } from "@/components/system/feature-status";
export default function Buy(){return <AppShell eyebrow="AIONEX TOKEN" title="Token access"><FeatureStatus title="Token sale is not configured" description="AIONEX does not expose a token sale, quote engine, contract address, or transaction flow in this deployment. No funds can be submitted from this page."/></AppShell>}
