import { healthCheck } from "@/lib/rewards/db";
export const dynamic="force-dynamic";
export async function GET(){try{const health=await healthCheck();const configured=Boolean(process.env.AUTH_SECRET&&process.env.AUTH_SECRET.length>=32&&process.env.DATABASE_URL&&process.env.TELEGRAM_BOT_TOKEN&&process.env.TELEGRAM_CHANNEL_ID&&process.env.TELEGRAM_ADMIN_CHAT_ID&&process.env.TELEGRAM_WEBHOOK_SECRET&&process.env.TELEGRAM_WEBHOOK_SECRET.length>=24);const ready=health.database&&health.migrations>=12&&configured;return Response.json({status:ready?"ok":"degraded",database:health.database,migrations:health.migrations,configured},{status:ready?200:503,headers:{"Cache-Control":"no-store"}});}catch{return Response.json({status:"unavailable",database:false,configured:false},{status:503,headers:{"Cache-Control":"no-store"}});}}
