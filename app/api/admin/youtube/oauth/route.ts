import { randomBytes } from "node:crypto";import { cookies } from "next/headers";import { getAdminSession } from "@/lib/admin/auth";import { youtubeAuthorizationUrl } from "@/lib/automation/youtube";
export async function GET(){if(!await getAdminSession()){const base=process.env.NEXT_PUBLIC_SITE_URL||"https://aionex-ai.vercel.app";return Response.redirect(`${base}/admin/login?next=${encodeURIComponent("/api/admin/youtube/oauth")}`);}const state=randomBytes(24).toString("base64url");const store=await cookies();store.set("youtube_oauth_state",state,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:600,path:"/"});return Response.redirect(youtubeAuthorizationUrl(state));}
