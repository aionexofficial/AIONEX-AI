import {openMysteryChest} from "@/lib/aion/gameplay";
import {exceedsContentLength,isSameOrigin} from "@/lib/http/request";
import {getRewardUserId} from "@/lib/rewards/session";
export async function POST(request:Request){if(!isSameOrigin(request))return Response.json({error:"Invalid request origin."},{status:403});if(exceedsContentLength(request,4096))return Response.json({error:"Request is too large."},{status:413});const userId=await getRewardUserId();if(!userId)return Response.json({error:"Authentication required."},{status:401});try{const body=await request.json();return Response.json({result:await openMysteryChest(userId,String(body.chestKey||""),String(body.idempotencyKey||""))});}catch(error){return Response.json({error:error instanceof Error?error.message:"Chest could not be opened."},{status:400});}}
