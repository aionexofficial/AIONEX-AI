export type StoryPresenter = "animated_core" | "human_avatar" | "voice_only" | "app_demo";
export type StoryScene = { position:number; purpose:string; startSeconds:number; endSeconds:number; narration:string; onScreenText:string; subtitleText:string; backgroundTemplateKey:string; visualInstructions:string[]; motionInstructions:string[]; assetReferences:string[] };
export type StoryScenario = { scenarioKey:string; categoryKey:string; topic:string; title:string; hook:string; narration:string; presenter:StoryPresenter; durationSeconds:number; thumbnailText:string; telegramCaption:string; youtubeTitle:string; youtubeDescription:string; xCaption:string; hashtags:string[]; callToAction:string; factualSafetyNotes:string[]; sourceReferences:string[]; scenes:StoryScene[] };
export type StorySettings = { generationEnabled:boolean; renderingEnabled:boolean; publishingEnabled:boolean; dryRun:boolean; previewOnly:boolean; dailyTime:string; timezone:string; durationSeconds:number; tone:string; voice:string; backgroundStyle:string; subtitleStyle:string; callToAction:string; repetitionThreshold:number; minimumQualityScore:number; dailyVideoLimit:number; emergencyPause:boolean; preferredCategories:string[]; blockedCategories:string[]; categoryWeights:Record<string,number> };
export type QualityDimensions = { relevance:number; originality:number; clarity:number; educationalValue:number; audienceValue:number; visualPotential:number; brandConsistency:number; factualSafety:number; hookStrength:number; callToAction:number };
export type QualityResult = { dimensions:QualityDimensions; totalScore:number; repetitionRisk:number; approved:boolean; rejectionReasons:string[] };
