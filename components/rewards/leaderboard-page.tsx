"use client";
import Link from "next/link";
import { useState } from "react";
import type { LeaderboardMetric } from "@/lib/rewards/types";
type Row={id:string;displayName:string;axp:number;xp:number;referrals:number;mining:number;tasks:number;rank:number};
const metrics:LeaderboardMetric[]=["xp","axp","referrals","mining","tasks"];
export function LeaderboardPage({initial}:{initial:Row[]}){const[metric,setMetric]=useState<LeaderboardMetric>("xp"),[rows,setRows]=useState(initial);async function choose(next:LeaderboardMetric){setMetric(next);const r=await fetch(`/api/rewards/leaderboards?metric=${next}`);if(r.ok)setRows((await r.json()).leaders);}return <main className="min-h-screen bg-[#020711] p-5 text-white"><div className="mx-auto max-w-5xl py-8"><Link href="/rewards" className="text-xs text-cyan-300">← Rewards</Link><h1 className="mt-4 text-5xl font-semibold">Community leaderboard</h1><p className="mt-3 text-slate-500">Ranked from canonical PostgreSQL reward, referral, mining, and task records.</p><div className="mt-7 flex flex-wrap gap-2">{metrics.map(m=><button key={m} onClick={()=>choose(m)} className={`rounded-xl px-4 py-2 text-sm capitalize ${metric===m?"bg-cyan-300 text-slate-950":"border border-white/10 text-slate-400"}`}>{m==="tasks"?"Completed Tasks":m}</button>)}</div><section className="mt-5 overflow-hidden rounded-2xl border border-white/10 bg-[#081321]"><div className="divide-y divide-white/5">{rows.map(r=><div key={r.id} className="grid grid-cols-[4rem_1fr_auto] items-center p-5"><span className="text-xl text-cyan-300">#{r.rank}</span><span>{r.displayName}</span><span className="font-mono text-sm text-slate-300">{r[metric].toLocaleString()} {metric.toUpperCase()}</span></div>)}</div></section></div></main>}
