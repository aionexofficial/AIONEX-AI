import { AppShell } from "../components/app-shell";import { FeatureStatus } from "@/components/system/feature-status";
export default function Governance(){return <AppShell eyebrow="AIONEX GOVERNANCE" title="Governance"><FeatureStatus title="On-chain governance is not active" description="No proposal contract, voting snapshot, or delegation provider is configured. The application does not display fabricated proposals or accept non-binding votes."/></AppShell>}
