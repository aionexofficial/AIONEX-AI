import { AppShell } from "../components/app-shell";import { FeatureStatus } from "@/components/system/feature-status";
export default function Portfolio(){return <AppShell eyebrow="AIONEX PORTFOLIO" title="Portfolio"><FeatureStatus title="Portfolio indexing is not configured" description="A production indexer and valuation source are required before wallet balances and performance can be shown. No simulated balances are displayed."/></AppShell>}
