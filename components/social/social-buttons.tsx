"use client";
import { useEffect,useState } from "react";
import { OFFICIAL_LINKS,SOCIAL_LABELS,type SocialProvider } from "@/lib/social/config";
const icons:Record<SocialProvider,string>={telegram:"✈",x:"𝕏",youtube:"▶"};
export function SocialButtons({compact=false,disabled=false}:{compact?:boolean;disabled?:boolean}){const[loading,setLoading]=useState<SocialProvider|null>(null),[links,setLinks]=useState<Record<SocialProvider,string>>({telegram:OFFICIAL_LINKS.telegram,x:OFFICIAL_LINKS.x,youtube:OFFICIAL_LINKS.youtube});useEffect(()=>{void fetch("/api/social-links").then(r=>r.ok?r.json():null).then(data=>{if(data?.links)setLinks(current=>({...current,...data.links}));});},[]);function open(provider:SocialProvider){if(disabled||loading)return;setLoading(provider);window.open(links[provider],"_blank","noopener,noreferrer");window.setTimeout(()=>setLoading(null),500);}return <div className="flex flex-wrap items-center gap-2">{(["telegram","x","youtube"] as const).map(provider=><button key={provider} type="button" disabled={disabled||loading!==null} aria-label={`Open AIONEX on ${SOCIAL_LABELS[provider]}`} onClick={()=>open(provider)} className="group inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border border-white/10 bg-white/[.04] px-3 text-xs text-slate-300 transition duration-200 hover:-translate-y-0.5 hover:border-cyan-300/50 hover:bg-cyan-300/[.08] hover:text-cyan-100 disabled:cursor-not-allowed disabled:opacity-40"><span className="transition group-hover:scale-110">{loading===provider?"…":icons[provider]}</span>{!compact&&SOCIAL_LABELS[provider]}</button>)}</div>}
